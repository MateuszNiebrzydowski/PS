#include <stdio.h>
#include <stdlib.h>

int suma(char n[], float x)
{
    int newX = (int)x;
    return newX;
}

int main()
{
    char nap[] = "Hello world!";
    printf("%d\n", suma(nap, 6.94));

    return 0;
}
